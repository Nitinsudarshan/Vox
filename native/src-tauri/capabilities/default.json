{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window, the floating dictation pill overlay, and the meeting reminder window. All three react entirely through events emitted from Rust \u2014 capture state and levels for the pill, meeting-reminder for the reminder window \u2014 so without core:event permissions their listen() calls are silently denied and the surface never reflects any backend state, no matter what the backend does.",
  "windows": [
    "main",
    "dictation-pill",
    "meeting-reminder"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "notification:default"
  ]
}
